import { languages } from "../jsonData/languages.json";

const langStyles = {
  display: "flex",
  flexFlow: "column wrap",
  maxHeight: "250px",
  paddingLeft: "16px",
};

const langInnerStyles = {
  padding: "8px",
};

export { languages, langStyles, langInnerStyles };
